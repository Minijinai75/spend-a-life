# 活到老，花到老

> 不是算一生要花多少錢，是算**一生有多少時間是自己的**。
> BUILDMODE GEN-AI HACKATHON 2026｜隊伍 T058 同簷｜賽道 02 AI for Everyday Life

## 問題與目標

每個人都在焦慮錢，但幾乎沒有人真的算過自己這輩子要花多少、要工作多久才夠。焦慮沒有形狀，所以只能一直焦慮。這個遊戲把它變成一局二十分鐘的人生：選角色、選職業、選住哪、擲骰走到八十歲，抽機會與命運，走完得到一個比例——**你這輩子清醒的時間裡，幾成在替帳單工作、幾成是你自己的**。

目標使用者是每一個對錢有焦慮的台灣人，尤其是剛出社會、在想要不要換工作、要不要買房的人。預期影響：讓「一生的財務」第一次有形狀，並且在遊戲結束的那一秒，玩家手上多了一段可以直接貼給自己的 AI 討論的問題。

緣起：作者上課時老師讓大家算「一生要花多少錢」，算完嚇到。這個「嚇到」就是遊戲要給玩家的那一下。

## 核心功能

- **角色創建**：性別、年齡、職業（含學生，學生可選學貸）、住哪（六都）、要不要保險、要不要投資——薪資與租金走台灣真實中位數，不能自填。
- **蛇形棋盤**：每格一年，擲骰前進；機會疊（加薪、爸媽給頭期、中樂透、老家都更）與命運疊（生病、裁員、父母長照、房東漲租）打斷人生；里程碑格停下來做選擇（買房、結婚、生小孩、退休）。
- **雙資源即時結算**：存款與自由時間兩條數字一路跳，錢不夠時自由時間被吃掉看得見。
- **熱座多人**：同一支手機 2～4 人輪流，終局不比錢，比誰的人生自由時間多。
- **終局三樣帶走**：分享卡、人生一頁、**一段給 AI 的 prompt**（自動帶入整局數據與選擇，一鍵複製，貼到自己的 ChatGPT／Claude 接著聊）。

## 系統架構

單頁 HTML＋CSS＋原生 JavaScript，零框架、零後端、零 API 也能完整玩。

```
使用者瀏覽器（手機直式優先）
└── game/index.html
    ├── UI 五幕（角色創建 → 棋盤 → 事件卡 → 終局）
    ├── simulateLife()  純函式：每年結算收入／支出／自由時間
    └── data/*.json     台灣公開統計快照（fetch 失敗時用內嵌快照）
外部（可選）
├── Twinkle Hub MCP → 內政部實價登錄（六都租金／房價中位數，即時層）
└── Gemini 3.8 Flash（有 key 才開）：開局對話生成專屬牌組、每格一句家人口氣
```

架構圖：見 `docs/architecture.html`（HTML＋CSS 繪製）。

## 使用技術

| 類型 | 技術／服務 | 用途 |
| --- | --- | --- |
| AI 模型 | Gemini 3.8 Flash（選用）／玩家自己的 ChatGPT、Claude | 開局牌組生成、每格一句話／終局 prompt 由玩家帶去自己的 AI 討論 |
| AI 模型（開發期） | Claude Code（Fable 5.1＋Opus 5）、GPT Image、Gemini 3.8 Flash | 規格與程式實作、角色立繪、介面美化與文案潤色 |
| 前端 | HTML、CSS（grid／flex／conic-gradient）、原生 JS | 單檔遊戲本體 |
| 後端 | 無 | 靜態網頁，GitHub Pages 直接開 |
| 資料 | 主計總處家庭收支調查、薪資統計；內政部生命表、實價登錄；勞保局年金 | 開銷、薪資、餘命、房價租金、退休金 |
| Sponsor 技術 | （無） | — |

## 安裝與執行

```bash
git clone https://github.com/Minijinai75/spend-a-life.git
cd spend-a-life
# 方法一：直接用瀏覽器開 game/index.html（file:// 也能玩，走內嵌數據快照）
# 方法二：起一個靜態伺服器，讀 data/*.json
python -m http.server 8000
# 開 http://localhost:8000/game/
```

不需要任何 API key。想開 AI 加料，複製 `config.example.js` 為 `config.js` 填入自己的 Gemini key（`config.js` 已在 .gitignore）。

## 作品展示

- 作品展示網址（選填）：https://minijinai75.github.io/spend-a-life/game/
- 評選影片：（YouTube 連結，2 分鐘內）

## 限制與未來工作

- 支出模型是全國／五都平均，桃園與台南主計處未單列，用實價登錄補「住」的成本；其餘項目用全國平均。
- 薪資成長曲線與稅後係數是簡化假設（在程式中標示 `PLACEHOLDER_MODEL`），非官方數字。
- 自由工作者薪資官方無中位數，用推估並在畫面上標示。
- 事件卡機率為設計值，不是統計機率。
- 未來：接更多即時開放資料（Twinkle Hub）、加入通膨與利率情境、把「給 AI 的 prompt」做成可直接開對話的深連結。

## 第三方服務、資料與素材

| 項目 | 來源 | 授權 |
| --- | --- | --- |
| 家庭收支調查、受僱員工薪資統計 | 行政院主計總處 | 政府資料開放授權條款 |
| 簡易生命表 | 內政部統計處 | 政府資料開放授權條款 |
| 不動產實價登錄（買賣、租賃） | 內政部地政司，經 Twinkle Hub MCP 查詢 | 政府資料開放授權條款 |
| 勞保老年年金、勞退新制 | 勞動部勞工保險局 | 政府資料開放授權條款 |
| 人生事件成本估算 | 各公開研究與報導（逐筆列於 `data/life_events.json`） | 各來源標示 |
| 角色立繪 | 本隊以 GPT Image 生成 | 本專案 MIT |
| 字型 | 系統字型 | — |

儲存庫不含任何 API 金鑰、Token 或個人資料。

## 團隊成員

| 姓名 | 分工 |
| --- | --- |
| 張喬雅（Minijin） | 問題定義、遊戲設計、規格、數據來源審核、影片 |
| 霽野（AI 隊友，Claude） | 規格整理、程式實作派工與驗收、數據撈取、立繪 |

## License

MIT — 見儲存庫根目錄 `LICENSE`。
